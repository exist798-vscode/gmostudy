# 학생 응답을 구글 스프레드시트로 수집하기

학생이 **‘제출하기’** 를 누르면 학습지 내용이 구글 스프레드시트에 한 줄씩 쌓이도록 설정한다.
서버는 필요 없고, 구글 Apps Script 웹 앱만 배포하면 된다. (약 5분)

---

## 1. 스프레드시트 만들기

1. <https://sheets.new> 로 새 구글 스프레드시트를 만든다.
2. 이름을 알아보기 쉽게 바꾼다. 예) `GMO 학습지 응답`.
   (시트 탭 이름은 그대로 둬도 된다. 스크립트가 `응답` 시트를 자동으로 만든다.)

## 2. Apps Script에 코드 붙여넣기

1. 스프레드시트 상단 메뉴: **확장 프로그램 → Apps Script**.
2. 편집기에 있던 `function myFunction() {}` 를 모두 지우고,
   이 저장소의 **`apps-script/Code.gs`** 내용을 통째로 붙여넣는다.
3. 저장(💾).

## 3. 웹 앱으로 배포

1. 오른쪽 위 **배포 → 새 배포**.
2. 톱니바퀴(유형 선택) → **웹 앱**.
3. 설정:
   - **설명**: 아무 값 (예: `gmo v1`)
   - **실행 계정**: **나**
   - **액세스 권한이 있는 사용자**: **모든 사용자**
     (학생 태블릿이 로그인 없이 보내야 하므로 반드시 이 옵션)
4. **배포** → 처음이면 권한 승인 창이 뜬다.
   “고급 → (프로젝트 이름)(으)로 이동 → 허용” 순서로 승인한다.
5. 배포 완료 화면의 **웹 앱 URL** 을 복사한다.
   `https://script.google.com/macros/s/AKfycb..../exec` 형태.

## 4. 앱에 URL 연결

1. 이 저장소의 **`config.js`** 를 열어 `submitUrl` 에 위 URL 을 붙여넣는다.

   ```js
   window.GMO_CONFIG = {
     submitUrl: "https://script.google.com/macros/s/AKfycb..../exec",
     token: ""
   };
   ```

2. 저장 후 커밋·푸시:

   ```bash
   git add config.js
   git commit -m "데이터 수집: Apps Script URL 연결"
   git push
   ```

3. Vercel 이 자동 재배포한다. (1분 내)

## 5. 확인

1. 배포된 사이트(<https://gmostudy.vercel.app>)를 새로고침한다.
2. 하단에 초록색 **‘제출하기’** 버튼이 보이면 연결된 것이다.
   (안 보이면 `config.js` 의 `submitUrl` 이 비었거나 오타)
3. 이름을 넣고 아무 칸이나 채운 뒤 **제출하기** → “제출되었습니다” 알림.
4. 스프레드시트에 **`응답`** 시트가 생기고 한 줄이 추가됐는지 확인한다.

---

## 수집되는 열

| 열 | 내용 |
|---|---|
| 제출시각 | 서버가 자동 기록 |
| 학교 / 학년 / 반 / 번호 / 이름 / 작성일자 | 학생 정보 |
| 안전성-찬성 / 안전성-반대 | 쟁점 1 |
| 환경과경제-찬성 / 환경과경제-반대 | 쟁점 2 |
| 식량문제-찬성 / 식량문제-반대 | 쟁점 3 |
| 먹거리선택권-찬성 / 먹거리선택권-반대 | 쟁점 4 |
| 나의입장 | 찬성 / 반대 / 아직 고민 중 |
| 그렇게생각하는까닭 | 자유 서술 |
| 새롭게알게된점 | 자유 서술 |

- 학생이 **다시 제출하면 새 줄로 추가**된다. (덮어쓰지 않음)
  같은 이름의 마지막 줄을 최종본으로 보면 된다.
- 학생 화면에는 “이미 제출했어요” 확인창이 떠서 실수로 중복 전송하는 것을 줄여 준다.

## (선택) 스팸 방지 토큰

`config.js` 는 공개 파일이라 URL 이 노출된다. 장난 전송이 걱정되면:

1. `apps-script/Code.gs` 의 `SHARED_TOKEN` 에 임의 문자열을 넣는다. 예: `SHARED_TOKEN = 'gmo-2026-3반'`
2. 같은 값을 `config.js` 의 `token` 에도 넣는다.
3. Apps Script는 **새 배포**(또는 기존 배포 관리 → 편집 → 버전 새로 만들기)로 다시 배포한다.
4. `config.js` 를 커밋·푸시한다.

토큰이 틀리면 서버가 응답을 거부한다. (완벽한 보안은 아니고, 낙서 방지 수준)

## 문제 해결

| 증상 | 확인할 것 |
|---|---|
| ‘제출하기’ 버튼이 안 보임 | `config.js` 의 `submitUrl` 값, 브라우저 새로고침(캐시) |
| “제출에 실패했어요” | 배포 시 **액세스 권한 = 모든 사용자** 인지, URL 끝이 `/exec` 인지(`/dev` 아님) |
| 시트에 한글이 깨짐 | 보통 정상. 구글 시트는 UTF-8. 셀 서식 문제면 열 너비만 조정 |
| 열이 밀림 | `응답` 시트 1행(헤더)을 지우지 말 것. 지웠으면 시트를 비우고 다시 제출 |
| 쟁점 이름을 바꿨다 | `index.html` 의 `ARGS` 를 바꾸면 헤더도 바뀐다. 기존 `응답` 시트는 비우고 새로 시작하는 게 깔끔 |
